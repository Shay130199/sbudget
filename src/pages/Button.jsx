import React, { Fragment } from 'react'
import './Login.css'

function Button() {
  return (
    <Fragment>
        <div>
        <button>Login</button>
    </div>
    </Fragment>

  )
}

export default Button
